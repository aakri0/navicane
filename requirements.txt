# ── Python dependencies for navicane ─────────────────────────
# Core ML / vision
opencv-python-headless>=4.8,<5.0
ultralytics>=8.0,<9.0
numpy>=1.24,<2.0

# Hardware interfaces (pure-Python — safe to install on any platform)
gpiozero>=2.0
smbus2>=0.4

# Camera (RPi-specific — installed separately in Dockerfile via system packages)
# picamera2  ← handled by apt, not pip

# TTS
# espeak     ← system package, not pip
